# Build output
/target

# macOS directory metadata
.DS_Store

# Python bytecode cache from the scripts/*.py gates.
__pycache__/
*.py[cod]

# Per-developer internal design docs, untracked by design; each
# dev keeps their own. The shared spine stays TRACKED:
# docs/design/kumbarium-design.md, docs/thesis.md,
# docs/decisions.md. (width_gate.py's whole-tree scan skips
# gitignored files, so these stay unlinted too.)
docs/design/internal/*

# Per-developer editor / analyzer config.
/rust-analyzer.toml
/.nvim.lua
