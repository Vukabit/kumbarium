# Vendored fork of [redacted]-util ([redacted] @ 848d14e). Deliberately a
# COPY, not a shared dependency: each project's utils diverge on
# purpose. Cross-port bugfixes by hand when they apply to both.

[package]
name = "kumbarium-util"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
thiserror.workspace = true
uuid.workspace = true
tempfile.workspace = true
fd-lock.workspace = true

[target.'cfg(unix)'.dependencies]
rustix = { workspace = true, features = ["fs"] }

[dev-dependencies]
proptest.workspace = true
