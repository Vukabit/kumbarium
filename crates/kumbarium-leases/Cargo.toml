# Leases: the reading room, the coordination section's third
# resource (docs/design/leases.md, D-043). What agents are
# DOING, right now: warn-never-block reservations with
# activity-renewed TTLs.

[package]
name = "kumbarium-leases"
version = "0.3.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
kumbarium-util = { path = "../kumbarium-util" }
rusqlite.workspace = true
thiserror.workspace = true
