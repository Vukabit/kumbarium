# The Library: schema, numbered migrations, entries, namespaces,
# FTS5 search plumbing, supersession, and (later) backups.

[package]
name = "kumbarium-store"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
kumbarium-util = { path = "../kumbarium-util" }
rusqlite.workspace = true
thiserror.workspace = true
