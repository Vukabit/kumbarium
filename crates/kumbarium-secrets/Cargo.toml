# The restricted stacks (docs/design/secrets.md, D-038/D-039):
# sealed credential custody. Witnessed access is the product;
# the vetted floor (XChaCha20-Poly1305 + zeroize) is the one
# admitted cryptography.

[package]
name = "kumbarium-secrets"
version = "0.4.4"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
chacha20poly1305.workspace = true
getrandom.workspace = true
kumbarium-util = { path = "../kumbarium-util" }
rusqlite.workspace = true
thiserror.workspace = true
zeroize.workspace = true
