# The brain: namespace resolution, retrieval ranking, dual scoring
# (relevance vs confidence), token budgeting, and the LLM curation
# trait (no implementation in v0.1).

[package]
name = "kumbarium-librarian"
version = "0.3.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
